CREATE TYPE languageName
FROM varchar(20) NOT NULL

CREATE TYPE weaponTypeName
FROM varchar(30) NOT NULL

CREATE TYPE armorWeightName
FROM varchar(30) NOT NULL

CREATE TYPE raceName
FROM varchar(30) NOT NULL

CREATE TYPE backgroundName
FROM varchar(30) NOT NULL

CREATE TYPE skillName
FROM varchar(30)

CREATE TYPE savingThrowName
FROM  varchar(30)


DROP TYPE languageName
DROP TYPE weaponTypeName
DROP TYPE armorWeightName
DROP TYPE raceName
DROP TYPE backgroundName
DROP TYPE skillName
DROP TYPE savingThrowName