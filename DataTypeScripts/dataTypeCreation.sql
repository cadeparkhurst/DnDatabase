CREATE TYPE languageName
FROM varchar(20) NOT NULL

CREATE TYPE weaponTypeName
FROM varchar(30)

CREATE TYPE armorWeightName
FROM varchar(30)

CREATE TYPE raceName
FROM varchar(30)

CREATE TYPE backgroundName
FROM varchar(30)